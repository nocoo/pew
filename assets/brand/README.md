# Pew identity

Locally adopted from `hexly.ai/artwork/logo-family/pew/2026-09-06-01/`, finishing `04`. The selected zebra preserves its wink, ivory and charcoal facets, and one rainbow tongue. Its neck enters naturally through the bottom and right edges. The lilac field keeps the owner's selected tapered-stripe motif.

| Asset | Role |
| --- | --- |
| `../../logo.png` | Canonical transparent foreground, 2048 × 2048 |
| `icon.png` | Square lilac presentation for platform-managed masking |
| `icon-rounded.png` | Rounded presentation for the 128 px README logo and social image |
| `provenance.json` | Source study, selected pass, local adoption scope, and hashes |

Regenerate all application derivatives with:

```sh
uv run --with pillow python scripts/resize-logos.py
```

The 24/80 px app marks, 32 px PNG favicon, and verified 16/32 px ICO entries use the transparent foreground. This includes both sidebar states, login, the landing-page coupon, leaderboard header, and downloadable badge previews. Keep these images free of a background tile or circular mask. The 180 px Apple touch icon uses the square presentation; the Open Graph image places the rounded presentation on the existing navy field.

The selected tile base is `#BFB2CF`. Ivory `#FCF8F0`, charcoal `#232637`, and the tongue accents come from sampled native pixels. Pew's existing purple UI primary `#851DED` remains a separate product color. At 16 px, the striped silhouette and one rainbow accent carry the identity; the wink and individual facets simplify.

[Local comparison and downloads](https://index.dev.hexly.ai/logos/pew) · [Local static study](https://index.dev.hexly.ai/artwork/logo-family/pew/2026-09-06-01/review.html)

Source adoption is local; this follow-up has not been pushed or deployed. Shared usage and adoption SOP: `hexly.ai/docs/07-logo-usage-sop.md`.
